import { defineConfig, devices } from '@playwright/test';

// True end-to-end tests: browser -> Angular dev server -> real backend at
// http://localhost:8080 (see StockValuationService.apiUrl). The backend is a
// separate service and is NOT started by this config — start it yourself
// before running `npx playwright test`.
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env['CI'],
  retries: process.env['CI'] ? 2 : 0,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:4200',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm start',
    url: 'http://localhost:4200',
    reuseExistingServer: !process.env['CI'],
    timeout: 120_000,
  },
});
